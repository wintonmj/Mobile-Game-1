export enum ServiceStatus {
    CREATED = 'created',
    STARTED = 'started',
    PAUSED = 'paused',
    RESUMED = 'resumed',
    SLEEPING = 'sleeping',
    WAKING = 'waking',
    SHUTDOWN = 'shutdown',
    DESTROYED = 'destroyed'
} 